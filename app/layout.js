import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Unlock Innovation and Optimize Efficiency in Operations | Feedback Form",
  description: "Unlock Innovation and Optimize Efficiency in Operations | Feedback Form",
   icons: {
    icon: '/vercel.svg',
    shortcut: '/vercel.svg',
    apple: '/apple-touch-icon.png', // Still need PNG for Apple devices
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
